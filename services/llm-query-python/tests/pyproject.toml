[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["B008"]  # FastAPI's Depends()/default-arg pattern triggers this; it's idiomatic here

[tool.ruff.format]
quote-style = "double"