# Template only — copy to secret.local.yaml (gitignored) and fill in real
# values before applying, or generate directly with kubectl:
#   kubectl create secret generic sentinelops-secrets \
#     --namespace sentinelops \
#     --from-literal=POSTGRES_USER=sentinelops \
#     --from-literal=POSTGRES_PASSWORD=devpassword \
#     --from-literal=POSTGRES_DB=sentinelops \
#     --from-literal=DATABASE_URL=postgres://sentinelops:devpassword@timescaledb:5432/sentinelops
apiVersion: v1
kind: Secret
metadata:
  name: sentinelops-secrets
  namespace: sentinelops
type: Opaque
stringData:
  POSTGRES_USER: sentinelops
  POSTGRES_PASSWORD: CHANGE_ME
  POSTGRES_DB: sentinelops
  DATABASE_URL: postgres://sentinelops:CHANGE_ME@timescaledb:5432/sentinelops